VictoriaMetrics
     ↓
autoscaler_controller.py → fetch + predict + log decisions
     ↓
record_live_data() → saves to /data/live_data/
     ↓
Daily CronJob → runs retrain_model.py
     ↓
Models get updated:
   gpr_model_<svc>.pkl
   xgb_model_<svc>.json
   lstm_model_<svc>.h5
     ↓
Next day → Better predictions!