smart-autoscaler/
│
├── config.py                  # Configuration for single service
├── config_multi.py            # Multi-service configuration
├── model_inference.py         # Main class: predict_scaling_action()
├── utils.py                   # Helper functions: create_sequence(), record_live_data()
├── autoscaler_controller.py   # Main loop – fetch metrics → predict → scale
├── retrain_models.py         # Online learning from live data
├── Dockerfile                 # For containerization
├── requirements.txt           # Python dependencies
└── cronjob.yaml               # K8s CronJob to run controller periodically